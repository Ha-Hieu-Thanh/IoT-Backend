export enum ERoleType {
  USER = 'user',
  ADMIN = 'admin',
}

export enum ETokenType {
  ACCESS_TOKEN = 'access_token',
  REFRESH_TOKEN = 'refresh_token',
}

export enum EStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}
